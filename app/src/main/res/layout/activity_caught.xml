<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:gravity="center"
    android:background="#FF424242"
    android:padding="32dp">

    <!-- Caught Icon/Image placeholder -->
    <ImageView
        android:layout_width="120dp"
        android:layout_height="120dp"
        android:src="@android:drawable/ic_delete"
        android:tint="#FFFF5252"
        android:layout_marginBottom="24dp"/>

    <!-- Caught Message -->
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="YOU GOT CAUGHT!"
        android:textSize="42sp"
        android:textStyle="bold"
        android:textColor="#FFFF5252"
        android:gravity="center"
        android:layout_marginBottom="16dp"/>

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Better luck next time!"
        android:textSize="18sp"
        android:textColor="#FFFFFFFF"
        android:gravity="center"
        android:layout_marginBottom="48dp"/>

    <!-- Spectate Button -->
    <com.google.android.material.button.MaterialButton
        android:id="@+id/btnSpectate"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Spectate Game"
        android:textSize="18sp"
        android:padding="16dp"
        android:layout_marginBottom="16dp"
        app:backgroundTint="#FF2196F3"
        android:textColor="#FFFFFFFF"
        app:icon="@android:drawable/ic_menu_view"
        app:iconGravity="textStart"/>

    <!-- Become Hunter Button -->
    <com.google.android.material.button.MaterialButton
        android:id="@+id/btnBecomeHunter"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Become Hunter"
        android:textSize="18sp"
        android:padding="16dp"
        android:layout_marginBottom="16dp"
        app:backgroundTint="#FFB71C1C"
        android:textColor="#FFFFFFFF"
        app:icon="@android:drawable/ic_menu_compass"
        app:iconGravity="textStart"/>

    <!-- Exit to Lobby Button -->
    <com.google.android.material.button.MaterialButton
        android:id="@+id/btnExitToLobby"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Exit to Lobby"
        android:textSize="18sp"
        android:padding="16dp"
        style="@style/Widget.Material3.Button.OutlinedButton"
        app:strokeColor="#FFFFFFFF"
        android:textColor="#FFFFFFFF"/>

</LinearLayout>
